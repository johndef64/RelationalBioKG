# Python dependencies of RelationalPKT (pinned to the verified environment).
# PyTorch and the compiled PyG extensions (torch_scatter, torch_sparse, pyg_lib, ...) are NOT listed
# here: their wheels depend on the CUDA version and are installed by create_env.sh.
# Full setup:  bash create_env.sh
torch-geometric==2.7.0
torcheval==0.0.7
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
ijson==3.5.0
wandb==0.25.1
tqdm==4.67.3
termcolor==3.3.0
requests
